{
  "name": "ticket-management-system",
  "version": "1.0.0",
  "description": "Ticket Management System with Customer and Company portals",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.js *.css dist/ 2>/dev/null || (md dist 2>nul & copy *.html dist\\ & copy *.js dist\\ & copy *.css dist\\) && echo 'Build completed successfully'",
    "start": "npx http-server . -p 8080 -o",
    "dev": "npx http-server . -p 8000 -o"
  },
  "keywords": ["ticket", "management", "html", "javascript", "customer", "portal"],
  "author": "Anish",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}